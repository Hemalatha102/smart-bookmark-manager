
# Smart Bookmark Manager

### Challenges Faced
* Encountered a 404 error on Vercel because files were not in the correct `app/` directory.
* Had build errors due to the `package.json` being in the wrong folder.

### Solutions
* Reorganized the repository to place `package.json` at the root.
* Created a proper Next.js App Router structure with `app/page.tsx`.
*
